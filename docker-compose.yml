services:
  soma:
    build: .
    ports:
      - "8080:8080"   # REST API
      - "4242:4242"   # MCP TCP
    volumes:
      - soma-data:/data
    environment:
      - SOMA_DATA_DIR=/data
      - RUST_LOG=info
    restart: unless-stopped

volumes:
  soma-data:
